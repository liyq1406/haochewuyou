<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@color/promote_white"
    android:id="@+id/frame_empty_sub_no_car"
    android:visibility="gone"
    style="@style/wh_match"
    >

  <TextView
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center"
      android:drawablePadding="@dimen/px_10dp"
      android:drawableTop="@drawable/icon_empty_car"
      android:text="@string/hc_empty_car_sub"
      android:textSize="@dimen/ip5_24px"
      android:textColor="@color/home_hot"
      />

</FrameLayout>