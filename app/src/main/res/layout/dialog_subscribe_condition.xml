<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@color/promote_white"
    android:orientation="vertical"
    style="@style/wh_match_wrap"
    >

  <include layout="@layout/fragment_subscribe_sub_condition_desc"/>

  <LinearLayout
      style="@style/wh_match_wrap"
      android:orientation="vertical"
      >

    <ListView
        android:id="@+id/lv_dsc"
        android:layout_weight="1.0"
        style="@style/wh_match_wrap"
        />

    <TextView
        android:id="@+id/tv_dsc_modify"
        android:layout_width="match_parent"
        android:layout_height="@dimen/filter_bar_height"
        android:background="@drawable/bg_top_line_gray"
        android:gravity="center"
        android:text="@string/hc_my_sub_manager"
        android:textColor="@color/reminder_red"
        />
  </LinearLayout>

</LinearLayout>